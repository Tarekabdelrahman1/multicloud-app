apiVersion: v1
kind: Service

metadata:
  name: workflow-service
  namespace: telecom-platform

spec:
  selector:
    app: workflow-service

  ports:
    - name: http
      port: 8080
      targetPort: 8080

  type: ClusterIP

apiVersion: v1
kind: Service

metadata:
  name: audit-service
  namespace: telecom-platform

spec:
  selector:
    app: audit-service

  ports:
    - name: http
      port: 8080
      targetPort: 8080

  type: ClusterIP

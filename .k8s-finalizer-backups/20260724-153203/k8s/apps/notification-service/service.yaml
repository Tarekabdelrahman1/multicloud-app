apiVersion: v1
kind: Service

metadata:
  name: notification-service
  namespace: telecom-platform

spec:
  selector:
    app: notification-service

  ports:
    - name: http
      port: 8080
      targetPort: 8080

  type: ClusterIP

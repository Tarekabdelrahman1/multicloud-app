apiVersion: v1
kind: Service

metadata:
  name: device-service
  namespace: telecom-platform

spec:
  selector:
    app: device-service

  ports:
    - name: http
      port: 8080
      targetPort: 8080

  type: ClusterIP

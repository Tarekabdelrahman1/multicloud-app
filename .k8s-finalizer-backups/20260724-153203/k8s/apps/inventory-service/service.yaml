apiVersion: v1
kind: Service

metadata:
  name: inventory-service
  namespace: telecom-platform

spec:
  selector:
    app: inventory-service

  ports:
    - name: http
      port: 8080
      targetPort: 8080

  type: ClusterIP
